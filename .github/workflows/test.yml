name: test

on:
  #push:
  # branches: 
  #    - main 
  # paths:
  #   - .github/workflows/test.yml
      
  # pull_request:
  #  branches: [ main ]

  workflow_dispatch:

jobs:

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      name: 'Lint and Build Bicep'
      with:
        inlineScript: |
          az bicep build --file ./eslz/customRoleDefinitions/mgRoleDefinitions.bicep
          az bicep build --file ./eslz/customRoleDefinitions/customRbacPeNetworkingPermissions.bicep
          az bicep build --file ./eslz/customRoleDefinitions/customRbacPrivateDnsAContributor.bicep

  validate:
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
    - uses: actions/checkout@v2
    
    - uses: azure/login@v1
      name: 'Login to Azure'
      with: 
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          environment: ${{ secrets.AZURE_ENVIRONMENT }}
          enable-AzPSSession: false
        
    - name: 'Set Azure Subscription'
      run: az account set --subscription ${{ secrets.AZURE_CONNSUBSCRIPTIONID }}

    - uses: Azure/cli@v1
      name: 'Run preflight validation'
      with:
        inlineScript: |
          az deployment sub validate \
            --name 'Deploy-Custom-Role-Definitions' \
            --location ${{ secrets.AZURE_LOCATION }} \
            --template-file ./eslz/customRoleDefinitions/customRbacPeNetworkingPermissions.bicep \
            --parameters ./eslz/customRoleDefinitions/.parameters/rgRbacPeNetworkingPermissions.json \ 
            --parameters location=${{ secrets.AZURE_LOCATION }}

  preview:
    runs-on: ubuntu-latest
    needs: [validate]
    steps:
    - uses: actions/checkout@v2
    
    - uses: azure/login@v1
      name: 'Login to Azure'
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        environment: ${{ secrets.AZURE_ENVIRONMENT }}
        enable-AzPSSession: false
      
    - name: 'Set Azure Subscription'
      run: az account set --subscription ${{ secrets.AZURE_CONNSUBSCRIPTIONID }}
              
    - uses: Azure/cli@v1
      name: 'Run what-if'
      with:
        inlineScript: |
          az deployment sub what-if \
            --name 'Deploy-Custom-Role-Definitions' \
            --location ${{ secrets.AZURE_LOCATION }} \
            --template-file ./eslz/customRoleDefinitions/customRbacPeNetworkingPermissions.bicep \
            --parameters ./eslz/customRoleDefinitions/.parameters/rgRbacPeNetworkingPermissions.json \
            --parameters location=${{ secrets.AZURE_LOCATION }}

  deploy:
    runs-on: ubuntu-latest
    needs: [preview]
    environment: Production
    steps:
    - uses: actions/checkout@v2
    
    - uses: azure/login@v1
      name: 'Login to Azure'
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        environment: ${{ secrets.AZURE_ENVIRONMENT }}
        enable-AzPSSession: false
      
    - name: 'Set Azure Subscription'
      run: az account set --subscription ${{ secrets.AZURE_CONNSUBSCRIPTIONID }}
              
    - uses: Azure/cli@v1
      name: 'Deploy - Custom Role Definitions'
      with:
        inlineScript: |
          az deployment sub create \
            --name 'Deploy-Custom-Role-Definitions' \
            --location ${{ secrets.AZURE_LOCATION }} \
            --template-file ./eslz/customRoleDefinitions/customRbacPeNetworkingPermissions.bicep \
            --parameters ./eslz/customRoleDefinitions/.parameters/rgRbacPeNetworkingPermissions.json \
            --parameters location=${{ secrets.AZURE_LOCATION }}
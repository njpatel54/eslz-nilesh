name: test - virtual networking

on:
 push:
  branches: 
     - main 
  paths:
    - .github/workflows/test.vnet.yml
    - test/virtualNetworks/*
    - test/deploy.bicep
      
  # pull_request:
  #  branches: [ main ]
 workflow_dispatch:

jobs:

  deploy:
    runs-on: ubuntu-latest
    #needs: [preview]
    environment: Production
    steps:
    - uses: actions/checkout@v2
    
    - uses: azure/login@v1
      name: 'Login to Azure'
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        environment: ${{ secrets.AZURE_ENVIRONMENT }}
        enable-AzPSSession: false
      
    - name: 'Set Azure Subscription'
      run: az account set --subscription ${{ secrets.AZURE_CONNSUBSCRIPTIONID }}

    - uses: Azure/cli@v1
      name: 'Deploy - Platform Hub VNet'
      with:
        inlineScript: |
          az group create --subscription ${{ secrets.AZURE_CONNSUBSCRIPTIONID }} --location ${{ secrets.AZURE_LOCATION }} --name ${{ secrets.AZURE_VNETRG }}
          az deployment group create \
            --name 'Deploy-Platform-Hub-VNet' \
            --resource-group ${{ secrets.AZURE_VNETRG }} \
            --template-file ./test/virtualNetworks/deploy.bicep \
            --parameters ./test/virtualNetworks/.parameters/hub.parameters.json

    - uses: Azure/cli@v1
      name: 'Deploy - Platform Spoke VNets'
      with:
        inlineScript: |
          az group create --subscription ${{ secrets.AZURE_MGMTSUBSCRIPTIONID }} --location ${{ secrets.AZURE_LOCATION }} --name ${{ secrets.AZURE_VNETRG }}
          az group create --subscription ${{ secrets.AZURE_IDENSUBSCRIPTIONID }} --location ${{ secrets.AZURE_LOCATION }} --name ${{ secrets.AZURE_VNETRG }}
          az group create --subscription ${{ secrets.AZURE_SSVCSUBSCRIPTIONID }} --location ${{ secrets.AZURE_LOCATION }} --name ${{ secrets.AZURE_VNETRG }}
          az deployment group create \
            --name 'Deploy-Platform-Spoke-VNets' \
            --resource-group ${{ secrets.AZURE_VNETRG }} \
            --template-file ./test/deploy.bicep \
            --parameters ./test/virtualNetworks/.parameters/plat.spoke.parameters.json \
            --parameters resourceGroupName=${{ secrets.AZURE_VNETRG }}